{
  "scripts": {
    "dev": "npx elm-live src/Main.elm --open --start-page=index.html --hot --pushstate -- --output=app.js",
    "prepare": "elm install"
  },
  "devDependencies": {
    "elm-format": "^0.8.5",
    "elm-review": "^2.7.5",
    "elm-test": "^0.19.1-revision9"
  }
}
